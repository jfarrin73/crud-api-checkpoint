spring:
  datasource:
    url: jdbc:mysql://localhost/crud_api_checkpoint?serverTimezone=UTC
    username: root
    password: 3L]cnN/g]L%{n3?p~q
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect